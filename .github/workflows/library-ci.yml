# Placeholder CI for loft-libs-assets — chunk has no packages yet.
#
# When the first package (glb 0.1.0) ships at W.0b, replace this
# entire file with the canonical `library-ci.yml` from the loft
# monorepo at
#   doc/claude/lib_plans/12-library-extraction/library-ci.yml.example
# and set the matrix `package: [glb]`.

name: library-ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "loft-libs-assets chunk bootstrapped 2026-05-31."
          echo "No packages yet — first inhabitant is glb (W.0b)."
          echo "When glb lands, replace this workflow with the canonical"
          echo "library-ci.yml template (see header for path)."
